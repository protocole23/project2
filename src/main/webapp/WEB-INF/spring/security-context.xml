<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<bean id="customAccessDenied" class="com.itwillbs.security.CustomAccessDeniedHandler" />
	<bean id="customLoginSuccess" class="com.itwillbs.security.CustomLoginSuccessHandler" />
	<bean id="customPasswordEncoder" class="com.itwillbs.security.CustomNoopPasswordEncoder" />
	
	<security:http auto-config="true" use-expressions="true">
		<security:intercept-url pattern="/home" access="permitAll" />
		<security:intercept-url pattern="/product/**" access="permitAll" />
		<security:intercept-url pattern="/resources/**" access="permitAll" />
		<security:intercept-url pattern="/chat/**" access="hasRole('ROLE_MEMBER')" />
		<security:intercept-url pattern="/user/join" access="permitAll" />
		<security:intercept-url pattern="/user/idCheck" access="permitAll" />
		
		<security:form-login 
		    login-page="/user/login" 
		    login-processing-url="/user/login"
		    username-parameter="userid"
		    password-parameter="userpw"
		    authentication-success-handler-ref="customLoginSuccess" />
		    
		<security:logout 
		    logout-url="/user/logout" 
		    invalidate-session="true" 
		    logout-success-url="/home" 
		    delete-cookies="JSESSIONID" />
			
		<security:access-denied-handler ref="customAccessDenied" />
        
        <security:csrf />
	</security:http>

	<security:authentication-manager>
	    <security:authentication-provider>
	        <security:password-encoder ref="customPasswordEncoder" />
	        <security:jdbc-user-service data-source-ref="dataSource"
	            users-by-username-query="select userid, userpw, (case when status = 0 then 1 else 0 end) as enabled from user where userid = ?"
	            authorities-by-username-query="select userid, 'ROLE_MEMBER' as auth from user where userid = ?" />
	    </security:authentication-provider>
	</security:authentication-manager>

</beans>