<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.EscrowMapper.">

	<insert id="createEscrow">
		INSERT INTO escrow (order_id, status, created_at)
		VALUES (#{orderId}, #{status}, NOW())
	</insert>

	<select id="getEscrow" resultType="com.itwillbs.domain.EscrowVO">
		SELECT * FROM escrow WHERE escrow_id = #{escrowId}
	</select>

	<update id="updateStatus">
		UPDATE escrow SET status = #{status}
		WHERE escrow_id = #{escrowId}
	</update>

</mapper>