<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.WalletMapper.">

	<select id="getWallet" resultType="com.itwillbs.domain.WalletVO">
		SELECT * FROM wallet WHERE user_id = #{userId}
	</select>

	<update id="updateBalance">
		UPDATE wallet
		SET balance = #{balance}, updated_at = NOW()
		WHERE user_id = #{userId}
	</update>

	<insert id="createWallet">
		INSERT INTO wallet (user_id, balance, updated_at)
		VALUES (#{userId}, 0, NOW())
	</insert>

</mapper>