<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.OrderMapper">

	<insert id="insertOrder">
		INSERT INTO `order`
		(product_id, buyer_id, status)
		VALUES
		(#{productId}, #{buyerId}, 0)
	</insert>

	<select id="getOrder" resultType="com.itwillbs.domain.OrderVO">
		SELECT
			order_id AS orderId,
			product_id AS productId,
			buyer_id AS buyerId,
			status
		FROM `order`
		WHERE order_id = #{orderId}
	</select>

	<select id="getOrderListByBuyer" resultType="com.itwillbs.domain.OrderVO">
		SELECT
			order_id AS orderId,
			product_id AS productId,
			buyer_id AS buyerId,
			status
		FROM `order`
		WHERE buyer_id = #{buyerId}
		ORDER BY order_id DESC
	</select>

	<update id="updateStatus">
		UPDATE `order`
		SET status = #{status}
		WHERE order_id = #{orderId}
	</update>

</mapper>