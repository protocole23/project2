<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.ReportMapper.">

	<insert id="createReport">
		INSERT INTO report (reporter_id, target_id, reason, created_at)
		VALUES (#{reporterId}, #{targetId}, #{reason}, NOW())
	</insert>

	<select id="getReportList" resultType="com.itwillbs.domain.ReportVO">
		SELECT * FROM report ORDER BY created_at DESC
	</select>

	<select id="getReport" resultType="com.itwillbs.domain.ReportVO">
		SELECT * FROM report WHERE report_id = #{reportId}
	</select>
	
</mapper>